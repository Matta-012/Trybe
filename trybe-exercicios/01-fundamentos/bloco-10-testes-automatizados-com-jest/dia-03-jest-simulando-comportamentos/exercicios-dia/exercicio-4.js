const toUpper = (string) => string.toUpperCase();

const firstChar = (string) => string[0];

const stringConcat = (str1, str2) => str1 + str2;

module.exports = { toUpper, firstChar, stringConcat };


